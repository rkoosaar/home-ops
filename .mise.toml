[env]
_.python.venv = { path = "{{config_root}}/.venv", create = true }
KUBECONFIG = "{{config_root}}/kubeconfig"
SOPS_AGE_KEY_FILE = "{{config_root}}/age.key"
TALOSCONFIG = "{{config_root}}/talos/clusterconfig/talosconfig"

[tools]
"python" = "3.14.7"
"pipx:makejinja" = "2.9.1"
# The kopiur CLI ships the `kubectl kopiur` plugin (snapshots list, status, doctor,
# logs, restore). Not in the aqua registry (verified against the generated
# registry.yaml), so it uses the github backend; keep this version in step with
# the operator chart in kubernetes/apps/kopiur-system, since the CLI talks to the
# CRDs of that release.
"github:home-operations/kopiur" = "0.10.9"
"aqua:derailed/k9s" = "0.51.0"
"aqua:budimanjojo/talhelper" = "3.1.17"
"aqua:cilium/cilium-cli" = "0.20.0"
"aqua:cli/cli" = "2.101.0"
"aqua:cloudflare/cloudflared" = "2026.9.3"
"aqua:cue-lang/cue" = "0.17.1"
"aqua:FiloSottile/age" = "1.3.2"
"aqua:fluxcd/flux2" = "2.9.5"
"aqua:getsops/sops" = "3.13.3"
"aqua:go-task/task" = "3.53.1"
"aqua:helm/helm" = "4.3.0"
"aqua:helmfile/helmfile" = "1.8.0"
"aqua:jqlang/jq" = "1.8.1"
"aqua:kubernetes-sigs/kustomize" = "5.7.1"
"aqua:kubernetes/kubectl" = "1.34.0"
"aqua:mikefarah/yq" = "4.53.6"
"aqua:siderolabs/talos" = "1.13.2"
"aqua:yannh/kubeconform" = "0.8.0"
