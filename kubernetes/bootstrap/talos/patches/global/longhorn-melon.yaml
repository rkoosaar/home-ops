machine:
  hostname: melon
  install:
    disks:
    - device: /dev/disk/by-id/usb-SSK_SSK_Storage_012345678913-0:0
      partitions:
      - mountpoint: /var/lib/longhorn

  kubelet:
    extraMounts:
      - destination: /var/lib/longhorn
        type: bind
        source: /var/lib/longhorn
        options:
          - bind
          - rshared
          - rw
