---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./ocirepository.yaml
  - ./helmrelease.yaml
configMapGenerator:
  - name: synapse-admin-configmap
    files:
      - config.json=./resources/config.json
generatorOptions:
  disableNameSuffixHash: true
