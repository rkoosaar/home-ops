# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: bedrockconnect
spec:
  chartRef:
    kind: OCIRepository
    name: bedrockconnect
  interval: 1h
  values:
    controllers:
      bedrockconnect:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: docker.io/pugmatt/bedrock-connect
              tag: 1.66.1@sha256:5880faf1761d308e357e349e8bf53c49fb63147d9bf0203a030a62c25553dc4d
            env:
              TZ: Europe/London
              BC_CUSTOM_SERVERS: /app/custom_servers.json
              BC_DEBUG: "true"
            probes:
              liveness:
                enabled: false
              readiness:
                enabled: false
              startup:
                enabled: false
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 10m
                memory: 64Mi
              limits:
                memory: 256Mi
    defaultPodOptions:
      automountServiceAccountToken: false
    service:
      app:
        type: LoadBalancer
        annotations:
          lbipam.cilium.io/ips: "10.1.1.49"
        ports:
          bedrock:
            port: 19132
            protocol: UDP
    persistence:
      app:
        type: configMap
        name: bedrockconnect-configmap
        advancedMounts:
          bedrockconnect:
            app:
              - path: /app/custom_servers.json
                subPath: custom_servers.json
                readOnly: true
