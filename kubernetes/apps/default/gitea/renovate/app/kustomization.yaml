---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - secret.sops.yaml
configMapGenerator:
  - name: renovate-configmap
    files:
      - config.json=./resources/config.json5
generatorOptions:
  disableNameSuffixHash: true
